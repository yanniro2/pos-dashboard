import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <section className="flex min-h-[20rem] w-full flex-1 items-center justify-center">
      <h1 className="text-primary">Check page</h1>
    </section>
  );
};

export default page;
